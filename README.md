# Тестовое задание для фронтенд-разработчика

Нужно сделать интерфейс магазина: каталог, корзину, оформление заказа и оплату тестовой картой. Бэкенд готов, фронтенд — в `apps/web`.

[Условия задания](docs/ASSIGNMENT.md) · [Работа с API](docs/INTEGRATION.md) · [Критерии оценки](docs/EVALUATION.md)

## Запуск

Потребуются Node.js 24.x и npm 11.x. Отдельная база данных и ключи внешних сервисов не нужны.

```sh
git clone https://github.com/instatdigital/frontend-checkout-challenge.git
cd frontend-checkout-challenge
npm ci
```

Два терминала:

1. API (`127.0.0.1:4000`) — корневой `npm run dev` запускает **только API**:

```sh
npm run dev
```

2. Фронтенд (Vite, порт 5173):

```sh
npm run dev -w @checkout/web
```

Сборка:

```sh
npm run build -w @checkout/web   # фронтенд
npm run build                    # API (и contracts)
```

Swagger: [http://localhost:4000/docs/](http://localhost:4000/docs/). Спецификация: [http://localhost:4000/openapi.json](http://localhost:4000/openapi.json) или [файл в репозитории](docs/openapi.json).

В Swagger выполните `POST /api/sessions` с телом `{}`. Скопируйте `data.token` в **Authorize**, без слова `Bearer`.

## Структура

```text
apps/api/             бэкенд
apps/web/             фронтенд `@checkout/web`
packages/contracts/   схемы API и типы TypeScript
docs/                 задание и документация
scripts/              проверки
```

Проект использует npm workspaces. Приложение в `apps/web` называется `@checkout/web`. Корневой `npm run dev` по-прежнему запускает только API; фронтенд — отдельной командой выше.

## Решение

Серверное состояние (товары, корзина, расчёт, заказ, оплата, sandbox) — TanStack Query. Клиентское — Zustand persist в `sessionStorage` с ключом `checkout.v1`: token, orderId, paymentId, черновик оформления, последние ключи идемпотентности. Суммы и состав корзины/заказа в Zustand не дублируются — только из API.

HTTP-клиент (D2): `apps/web/src/shared/api/client.ts` (базовый URL, `Authorization`, JSON/`204`). Ошибки — `errors.ts`, пути запросов — `endpoints.ts`. Страницы не копируют настройку `fetch`.

## Проверки

```sh
npm run check       # форматирование, сборка, тесты и OpenAPI
npm run build
npm start           # запуск собранного бэкенда
```

При работающем API в другом терминале выполните `npm run smoke`. Эта команда проверяет покупку, отказ карты, отмену и повтор оплаты по HTTP.

## Настройки

Адрес по умолчанию — `127.0.0.1:4000`. Если порт занят, скопируйте `.env.example` в `.env` и измените `PORT`. Для проверки другого порта передайте `BASE_URL`, например `BASE_URL=http://localhost:4100 npm run smoke`.

Фронтенд может работать на любом HTTP-порту `localhost`, `127.0.0.1` или `[::1]`. Другие разрешённые адреса задаются в `CORS_ORIGINS`. Авторизация передаётся заголовком; cookies и `credentials: include` не нужны.

Данные сохраняются в `.data/store.json`. Запускайте один экземпляр API на один файл. Для сброса остановите сервер и выполните `npm run data:reset`; затем создайте новую сессию. Если меняли `DATA_FILE`, свой файл удалите вручную при остановленном сервере.

Товары и адреса вымышленные. Остаток ограничивает количество в одной корзине и не уменьшается у других покупателей. Для получателя используйте тестовые контакты, например `buyer@example.test`. Вместо ввода номера карты интерфейс должен предлагать тестовые карты из API.

Корневые команды сборки и тестов проверяют бэкенд. Сборка фронтенда: `npm run build -w @checkout/web`. Схемы API в `packages/contracts` можно использовать напрямую или описать нужные типы у себя.

## Проверенные сценарии

В этом прогоне проверялись typecheck, format и code review, **не** ручной проход в браузере. Пункты ниже **не** отмечены как пройденные.

| Пункт | Что проверяется | Статус |
| ----- | --------------- | ------ |
| A1 | Каталог из API; доступный товар в серверную корзину | не проверялся в UI |
| A2 | Количество, удаление, состав и итог корзины совпадают с API | не проверялся в UI |
| A3 | Контакты и оба способа доставки с нужными полями | не проверялся в UI |
| A4 | Смена товаров/доставки даёт новый расчёт; суммы из API | не проверялся в UI |
| A5 | Заказ с выбранной оплатой; карта — название и маска из API | не проверялся в UI |
| A6 | Ожидание оплаты; успех по итоговому статусу заказа на сервере | не проверялся в UI |
| A7 | Страница успеха: номер, товары, доставка и сумма этого заказа | не проверялся в UI |
| A8 | Наличные: оплата при получении, без онлайн-оплаты | не проверялся в UI |
| B1 | Отказ и отмена различимы; повторная оплата того же заказа | не проверялся в UI |
| B2 | Двойное нажатие и повтор запроса не создают второй заказ/попытку | не проверялся в UI |
| B3 | Перезагрузка восстанавливает сессию, корзину и заказ | не проверялся в UI |
| B4 | Конфликт версии корзины / устаревший расчёт — можно продолжить | не проверялся в UI |
| B5 | Сбой сети/API не стирает форму; старый ответ не подменяет новые данные | не проверялся в UI |
| B6 | Пустая корзина, недоступный товар, остаток; опрос оплаты останавливается | не проверялся в UI |
| C1 | Загрузка, ожидание оплаты, ошибка и успех различимы | не проверялся в UI |
| C2 | Подписи полей, ошибки у полей, форма с клавиатуры | не проверялся в UI |
| C3 | Сценарий при ширине 1280 и 390 px | не проверялся в UI |
| D1 | Компоненты и типы читаемы; данные API, UI и действия различимы | не проверялся в UI |
| D2 | Общий HTTP-клиент: `apps/web/src/shared/api/client.ts` (+ `errors.ts`, `endpoints.ts`) | не проверялся в UI |
| E1 | Клон по README: install, API, фронтенд, сборка; lockfile, сценарии, недоработки, время | не проверялся в UI |

## Недоработки

- В сводке заказа для самовывоза показывается `pickupPointId`, а не название пункта.
- Biome предупреждает на `buttonVariants` в `apps/web/src/shared/ui/button.tsx`.
- Расчёт (quote) хранится в `useState` на странице оформления, не в TanStack Query.
- Нет Playwright (и других e2e) — сценарии A–E вручную в UI в этом прогоне не гонялись.

## Затраченное время

NEED_INPUT
